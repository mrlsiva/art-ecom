import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.siawalls.com',
  appName: 'SIA WALLS',
  webDir: 'dist/art-ecom-app/browser',
};

export default config;
